import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'drone-hub-mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
